/**
 * 产品名的唯一来源（JS 侧）。
 *
 * ⚠️ **这个名字现在还是暂用的 —— 小程序名还没定。**
 * 但有一条约束必须提前记住：**小程序名称全平台唯一，注册/发布时写死，
 * 微信里给用户显示的就是它。** 所以等名字定下来之后，这里和 app.json
 * 两份必须**一字不差**：差一个字，微信里显示注册名、卡片和标题里写的是另一个，
 * 演示的时候会被看出来。
 * 这一条没有脚本能守 —— 没有任何本地文件知道注册名 —— 只能靠人核对一次。
 *
 * 改名字要动的地方**只有两处**：
 *   1. 这个文件
 *   2. app.json 的 window.navigationBarTitleText
 *      （app.json 是纯 JSON，import 不进来，这一处只能硬编码 —— 整个项目唯一
 *       允许重复的地方，改完记得两边对一遍）
 * 成本对比：改这里的成本是 0；改**注册名**要消耗每年 2 次改名机会之一
 * （发布前 2 次，发布后 2 次/年）。所以名字一旦注册好就别再动，两边以注册名为准。
 *
 * 检查脚本会读这里，并在 README 里被提到，不会散落到别处。
 */

const APP_NAME = '收手吧';

/** 卡片底部那行小字。它不出现在小程序界面上，只在分享出去的图片里 */
const CARD_FOOTER = '把诱导多花钱的设计，一个个认出来';

/** 转发时好友看到的那句话 */
const SHARE_TITLE = '这张图里，有几处是在催你下单？';

module.exports = {
  APP_NAME: APP_NAME,
  CARD_FOOTER: CARD_FOOTER,
  SHARE_TITLE: SHARE_TITLE
};
