/**
 * 三段示例的数据。
 *
 * 这里**只写位置，不写文案**：
 * 陷阱名和机制句一律从 utils/hooks.js 取（唯一来源）。
 * 改文案 → 改词典；改位置 → 改这里。两件事不要混在一个文件里。
 *
 * rect 是相对整张图的 0–1 归一化比例（左上原点），
 * 数值是用裁剪核对过的，不是"看着差不多"填的。
 */

const SHOTS = [
  {
    id: 'shot-01',
    // 教：卡片上写着陷阱名，点一下看它在哪
    mode: 'reveal',
    appName: '电商商品页',
    image: '/assets/samples/01.jpg',
    hooks: [
      { id: 'A1', rect: { x: 0.015, y: 0.544, w: 0.97, h: 0.079 } },
      // 只框划线价那一小段，不框当前价 ——「按住」时划线价要消失，当前价格必须留下，
      // 那个对比（原价没了、现价还在）才是这个陷阱被读懂的一刻。
      // 边界是按原图裁出来量的：¥299 占 x 187–268、y 858–881（原图 750×1125），
      // 左边界必须避开左边的现价（到 x≈162 为止）。
      { id: 'B1', rect: { x: 0.24, y: 0.758, w: 0.126, h: 0.028 } }
    ]
  },
  {
    id: 'shot-02',
    // 练：先替你揭示凑单条，再问「还有一处，你觉得在哪？」
    mode: 'find-one',
    appName: '购物车页',
    image: '/assets/samples/02.jpg',
    hooks: [
      { id: 'C3', rect: { x: 0.02, y: 0.076, w: 0.96, h: 0.066 } },
      { id: 'D1', rect: { x: 0.02, y: 0.548, w: 0.96, h: 0.245 } }
    ]
  },
  {
    id: 'shot-03',
    // 放手：一处都不给，全靠自己找（有「直接显示」兜底）
    mode: 'find-all',
    appName: '内容平台种草帖',
    image: '/assets/samples/03.jpg',
    hooks: [
      { id: 'D2', rect: { x: 0.03, y: 0.798, w: 0.95, h: 0.111 } },
      { id: 'D1', rect: { x: 0.46, y: 0.924, w: 0.53, h: 0.058 } }
    ]
  }
];

module.exports = { SHOTS: SHOTS };
